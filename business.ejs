<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Business News</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@1.9.6/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <header class="bg-white shadow">
        <div class="container mx-auto flex justify-between items-center py-4 px-6">
            <h1 class="text-2xl font-bold text-gray-800">Business News</h1>
            <nav class="flex space-x-4">
                <a href="/" class="text-blue-500 hover:underline">Home</a>
                <a href="/science" class="text-blue-500 hover:underline">Science</a>
                <a href="/technology" class="text-blue-500 hover:underline">Technology</a>
                <a href="/sports" class="text-blue-500 hover:underline">Sports</a>
                <a href="/business" class="text-blue-500 font-semibold underline">Business</a>
            </nav>
        </div>
    </header>

    <div class="container mx-auto mt-8">
        <h2 class="text-xl font-bold mb-6">Latest Business News</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% if (articles && articles.length > 0) { %>
                <% articles.forEach(article => { %>
                    <div class="bg-white p-4 rounded-md shadow-md">
                        <h3 class="text-lg font-semibold mb-2">
                            <a href="<%= article.link %>" target="_blank" class="text-blue-500 hover:underline"><%= article.title %></a>
                        </h3>
                        <p class="text-gray-700 mb-2"><%= article.contentSnippet %></p>
                        <p class="text-gray-500 text-sm">
                            Source: <%= article.source %>
                        </p>
                    </div>
                <% }) %>
            <% } else { %>
                <p class="text-gray-500">No business news available at the moment.</p>
            <% } %>
        </div>
        <a href="/" class="mt-6 inline-block bg-blue-500 text-white px-4 py-2 rounded-md">Back to Home</a>
    </div>

    <footer class="mt-10 bg-white shadow p-4 text-center">
        <p class="text-gray-500">&copy; 2024 News App. All rights reserved.</p>
    </footer>
</body>
</html>
