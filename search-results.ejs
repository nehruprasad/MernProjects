<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto mt-10">
        <h1 class="text-2xl font-bold">Search Results</h1>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 mt-4">
            <% articles.forEach(article => { %>
                <div class="mb-4 p-4 border border-gray-300 rounded-md bg-white shadow-lg">
                    <!-- Display the article image -->
                    <img 
                        src="<%= article.urlToImage ? article.urlToImage : '/path/to/fallback-image.jpg' %>" 
                        alt="News Image" 
                        class="w-full h-48 object-cover mb-4"
                        onerror="this.src='/path/to/fallback-image.jpg';"
                    >
                    <!-- Article Title -->
                    <h2 class="text-xl font-semibold">
                        <a href="<%= article.url %>" target="_blank" class="text-blue-500 hover:underline">
                            <%= article.title %>
                        </a>
                    </h2>
                    <!-- Article Description -->
                    <p class="mt-2 text-gray-700"><%= article.description %></p>
                    <!-- Published Date -->
                    <p class="text-gray-500 mt-2">
                        <small>Published at: <%= new Date(article.publishedAt).toLocaleString() %></small>
                    </p>
                </div>
            <% }) %>
        </div>
        <!-- Back to News button -->
        <a href="/" class="mt-4 inline-block bg-blue-500 text-white px-4 py-2 rounded-md">Back to News</a>
    </div>
</body>
</html>
